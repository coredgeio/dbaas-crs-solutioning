# DBaaS Monitoring API Configuration
# 
# PROMETHEUS_URL is OPTIONAL - The API will auto-discover it from your Kubernetes cluster
# If auto-discovery fails, it will use the value below

# === Manual Configuration (Highest Priority) ===
# Uncomment to override auto-discovery
# PROMETHEUS_URL=http://172.16.109.237:31492

# === Auto-Discovery Configuration ===
# These values are used if PROMETHEUS_URL is not set
# Customize these for your cluster

# Kubernetes namespace where Prometheus is running
PROMETHEUS_NAMESPACE=monitoring

# Prometheus service name
PROMETHEUS_SERVICE=kube-prom-stack-kube-prome-prometheus

# === API Server Configuration ===
PORT=8080
ENVIRONMENT=development

# === How to Use ===
# 
# Option 1: Direct URL (bypasses auto-discovery)
#   export PROMETHEUS_URL=http://192.168.1.100:30000
#   go run main.go
#
# Option 2: Auto-discovery with custom namespace/service
#   export PROMETHEUS_NAMESPACE=my-monitoring
#   export PROMETHEUS_SERVICE=my-prometheus-svc
#   go run main.go
#
# Option 3: Use defaults (monitoring namespace)
#   go run main.go

